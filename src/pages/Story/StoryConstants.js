const STATUS = {
  values: ["TODO", "DEV", "QA", "COMPLETED"],
};

const PRIORITY = {
  values: ["NONE", "LOW", "MEDIUM", "HIGH", "URGENT"],
};

const NO_SPRINT = -1;

export { STATUS, PRIORITY, NO_SPRINT };
